export default class Task {
 constructor(name,description,priority,date) {
  this.taskName = name
  this.taskDescriptioin = description
  this.taskPriorityLevel = priority
  this.taskDeadline = date
 }
}